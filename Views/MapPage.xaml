<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Microsoft.Maui.Controls.Maps;assembly=Microsoft.Maui.Controls.Maps"
             x:Class="tortugueroApp.Views.MapPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource BackgroundLight}">
    
    <Grid RowDefinitions="Auto,*">
        
      
        <Frame Grid.Row="0"
               Padding="16"
               CornerRadius="0"
               HasShadow="True"
               BorderColor="Transparent"
               BackgroundColor="#CCF7F5F2">
            <Grid ColumnDefinitions="48,*,48">
                <Button Grid.Column="0"
                       Text="←"
                       FontSize="24"
                       TextColor="{StaticResource ForestGreen}"
                       BackgroundColor="Transparent"
                       Clicked="OnBackClicked" />
                
                <Label Grid.Column="1"
                       Text="Map Location"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="{StaticResource ForestGreen}"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center" />
            </Grid>
        </Frame>

     
        <Grid Grid.Row="1">
            <maps:Map x:Name="TortugueroMap"
                      IsShowingUser="True"
                      MapType="Hybrid" />
            
            
            <Frame Padding="16"
                   CornerRadius="12"
                   HasShadow="True"
                   BorderColor="Transparent"
                   BackgroundColor="{StaticResource ForestGreen}"
                   HorizontalOptions="Center"
                   VerticalOptions="Start"
                   Margin="16,16,16,0">
                <VerticalStackLayout Spacing="4">
                    <Label Text="🏨 Pachira Lodge"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalTextAlignment="Center" />
                    <Label Text="Tortuguero, Costa Rica"
                           FontSize="12"
                           TextColor="#E6FFFFFF"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </Frame>
            
          
            <VerticalStackLayout Spacing="12"
                                 HorizontalOptions="Start"
                                 VerticalOptions="End"
                                 Margin="16,0,0,16">
                
                
                <Frame Padding="12"
                       CornerRadius="8"
                       HasShadow="True"
                       BorderColor="Transparent"
                       BackgroundColor="{StaticResource ForestGreen}">
                    <Label Text="🧭"
                           FontSize="20">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnNavigateToLodge" />
                        </Label.GestureRecognizers>
                    </Label>
                </Frame>
                
              
                <Frame Padding="12"
                       CornerRadius="8"
                       HasShadow="True"
                       BorderColor="Transparent"
                       BackgroundColor="White">
                    <Label Text="🗺️"
                           FontSize="20">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnMapTypeToggled" />
                        </Label.GestureRecognizers>
                    </Label>
                </Frame>
                
              
                <Frame Padding="12"
                       CornerRadius="8"
                       HasShadow="True"
                       BorderColor="Transparent"
                       BackgroundColor="White">
                    <Label Text="📌"
                           FontSize="20">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCenterLocation" />
                        </Label.GestureRecognizers>
                    </Label>
                </Frame>
            </VerticalStackLayout>
        </Grid>
        
    </Grid>
</ContentPage>
